<%= form_with(model: mysurvey) do |form| %>
  <% if mysurvey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mysurvey.errors.count, "error") %> prohibited this mysurvey from being saved:</h2>

      <ul>
        <% mysurvey.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :laptop %>
    <%= form.text_field :laptop %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
